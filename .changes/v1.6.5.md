## v1.7.0 - 2026-03-24
### Added
* UpdateInstanceData added omitempty to avoid JSON having "" which may overwrite existing values. 
### Changed
* moved away from path.Join to fmt.Sprintf for building URLs path due to behaviour of path.Join on windows
* Version is set to omit if empty when creating an instance as this is not required
* AuraAPIClientVersion updated
* README.md has references to v2 removed 
* Split Overwrite into OverwriteFromInstance and OverwriteFromSnapshot to improve usability.  Adjusted tests to match
